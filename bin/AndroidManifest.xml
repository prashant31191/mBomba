<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bomba.b"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Sherlock.Light.DarkActionBar" >
        <activity
            android:name=".MainActivity"
            android:label="@string/playlists" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddPlaylist"
            android:label="Create Playlist"
            android:theme="@style/Sherlock.__Theme.Dialog" >
        </activity>
        <activity android:name=".Searchy" >
        </activity>
        <activity android:name=".ArtistAbout" >
        </activity>
        <activity android:name=".SideSearch" >
        </activity>
    </application>

</manifest>